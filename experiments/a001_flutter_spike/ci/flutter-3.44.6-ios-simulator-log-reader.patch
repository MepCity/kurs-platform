diff --git a/packages/flutter_tools/lib/src/ios/simulators.dart b/packages/flutter_tools/lib/src/ios/simulators.dart
index 4af0bf29622..ecae1d9115b 100644
--- a/packages/flutter_tools/lib/src/ios/simulators.dart
+++ b/packages/flutter_tools/lib/src/ios/simulators.dart
@@ -479,0 +480,4 @@ class IOSSimulator extends Device {
+      final DeviceLogReader logReader = getLogReader(app: package);
+      if (logReader is _IOSSimulatorLogReader) {
+        await logReader.start();
+      }
@@ -481 +485 @@ class IOSSimulator extends Device {
-        getLogReader(app: package),
+        logReader,
@@ -823 +827 @@ class _IOSSimulatorLogReader extends SharedIOSDeviceLogReader {
-    onListen: _start,
+    onListen: _connectProcessOutput,
@@ -826,0 +831,3 @@ class _IOSSimulatorLogReader extends SharedIOSDeviceLogReader {
+  bool _started = false;
+  bool _usesUnifiedLogging = false;
+
@@ -841 +848,5 @@ class _IOSSimulatorLogReader extends SharedIOSDeviceLogReader {
-  Future<void> _start() async {
+  Future<void> start() async {
+    if (_started) {
+      return;
+    }
+    _started = true;
@@ -843 +854,2 @@ class _IOSSimulatorLogReader extends SharedIOSDeviceLogReader {
-    if (await device.sdkMajorVersion >= 11) {
+    _usesUnifiedLogging = await device.sdkMajorVersion >= 11;
+    if (_usesUnifiedLogging) {
@@ -845,2 +856,0 @@ class _IOSSimulatorLogReader extends SharedIOSDeviceLogReader {
-      _deviceProcess?.stdout.transform(utf8LineDecoder).listen(_onUnifiedLoggingLine);
-      _deviceProcess?.stderr.transform(utf8LineDecoder).listen(_onUnifiedLoggingLine);
@@ -851,2 +860,0 @@ class _IOSSimulatorLogReader extends SharedIOSDeviceLogReader {
-      _deviceProcess?.stdout.transform(utf8LineDecoder).listen(_onSysLogDeviceLine);
-      _deviceProcess?.stderr.transform(utf8LineDecoder).listen(_onSysLogDeviceLine);
@@ -857,0 +866,11 @@ class _IOSSimulatorLogReader extends SharedIOSDeviceLogReader {
+  }
+
+  void _connectProcessOutput() {
+    if (_usesUnifiedLogging) {
+      _deviceProcess?.stdout.transform(utf8LineDecoder).listen(_onUnifiedLoggingLine);
+      _deviceProcess?.stderr.transform(utf8LineDecoder).listen(_onUnifiedLoggingLine);
+    } else {
+      _deviceProcess?.stdout.transform(utf8LineDecoder).listen(_onSysLogDeviceLine);
+      _deviceProcess?.stderr.transform(utf8LineDecoder).listen(_onSysLogDeviceLine);
+    }
+
